function GetSystemPageSize() {
  let page_size = 0;
  return page_size;
}

let secure_allocator = {
  base: null,
  // TODO: find struct def
}

let zero_after_free_allocator = {
  base: null,
  // TODO: find struct def 
}
